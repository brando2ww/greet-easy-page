

## Mover imagem do carregador mais para baixo no card

### Mudança

**`src/pages/Home.tsx`** (linha 48):
- Trocar `top-2` por `top-8` para descer a imagem dentro do card.

```tsx
className="absolute top-8 left-4 h-20 object-contain pointer-events-none"
```

